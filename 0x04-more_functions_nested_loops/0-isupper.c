#include <ctype.h>
#include "main.h"
/**
*_isupper - isupper funvtion
*@c: input
*Description: check if c is uper or ot
*Return: 1 if upper and 0 of not
*/
int _isupper(int c)
{
if (isupper(c))
	return (1);
return (0);
}
