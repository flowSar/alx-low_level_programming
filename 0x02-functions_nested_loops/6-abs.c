#include "main.h"
#include <stdlib.h>
/**
* _abs -abs
* @x: input
*
* Description: computes the absolute value of an integer x
* Return: return the vaue of abs
*/
int _abs(int x)
{
	int y = abs(x);

	return (y);
}
