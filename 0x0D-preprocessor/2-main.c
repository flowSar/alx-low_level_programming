#include <stdio.h>
/**
 * main -  entry function.
 * Description: print the name of the file that compli from.
 * Return: o if it success and 1 if it fails.
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
