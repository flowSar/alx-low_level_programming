#include <string.h>
#include "main.h"
#include <stdio.h>
/**
 * _strcpy - print half of a string
 * @dest : input
 * @src: input
 *
 * Return: the value of dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
