#include <string.h>
#include "main.h"
#include <string.h>
/**
 * puts2 - sget length of a tring
 * @str : input
 *
 * Return: the length ofstring
 */
void puts2(char *str)
{
	int i, l;

	l = strlen(str);
	for (i = 0 ; i > l; i--)
	{
		putchar(s[i]);
	}
	putchar('\n');
}
