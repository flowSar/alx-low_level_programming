#include <stdio.h>
#include <string.h>
/**
 * _strcmp - sget length of a tring
 * @s1 : input
 * @s2 : input
 *
 * Return: 0 if success
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	strcmp(s1, s2);
	return (result);
}
