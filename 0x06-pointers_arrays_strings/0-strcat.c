#include <stdio.h>
#include <string.h>
/**
 * _strcat - sget length of a tring
 * @dest : input
 * @src : input
 *
 * Return: 0 if success
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	return ('0');
}
