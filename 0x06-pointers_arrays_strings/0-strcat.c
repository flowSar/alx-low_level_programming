#include <stdio.h>
#include <string.h>
/**
 * _strcat - sget length of a tring
 * @s : input
 *
 * Return: the length ofstring
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	return ('0');
}